/**
 * A small, neutral set of icons and colors for the decoy disguise.
 *
 * Deliberately generic system-utility glyphs and standard iOS accent
 * colors — nothing decorative, nothing thematic, nothing that reads as
 * "for men" or "for women." The person choosing a disguise picks what
 * blends in on their own phone; we just supply a plain, unopinionated set
 * of options rather than any pre-built persona.
 */
export const DECOY_ICON_OPTIONS = ['📝', '📋', '✓', '🗂', '⏱', '⚙'];

export const DECOY_COLOR_OPTIONS = [
  '#8E8E93', // gray
  '#007AFF', // blue
  '#34C759', // green
  '#AF52DE', // purple
  '#FF9500', // orange
  '#5AC8FA', // teal
];
